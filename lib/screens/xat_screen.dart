import 'package:flutter/material.dart';

class XatScreen extends StatelessWidget {
  const XatScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Xat Screen'),
    );
  }
}
